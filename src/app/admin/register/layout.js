/**
 * Standalone layout for authentication pages
 * This prevents the admin sidebar/layout from appearing on registration pages
 */
export default function AuthLayout({ children }) {
  return children;
}
